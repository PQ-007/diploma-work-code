module.exports = {
  darkMode: "class",
  theme: {
    extend: {
      backgroundColor: {
        background: "var(--background)",
      },
    },
  },
  plugins: [require("@tailwindcss/typography")],
};
